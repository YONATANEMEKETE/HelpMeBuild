import React from 'react';
import Container from '../Container';
import Nav from './Nav';

const Hero = () => {
  return (
    <Container className="min-h-screen">
      <Nav />
    </Container>
  );
};

export default Hero;
